# API Testing Documentation

This project uses a small in-memory Node.js API to manage accounts. Tests in `test/` exercise create, list, retrieve, update and delete flows.

Run tests and coverage:

```powershell
npm install
npm test
npm run coverage
```

